# Example environment — copy to .env (never commit .env)
TRANSPORT=stdio
SEC_CONTACT_EMAIL=you@example.com
PORT=8787
HOST=0.0.0.0
API_KEYS=
ALLOW_ANONYMOUS=true
ANONYMOUS_RPM=30
AUTHENTICATED_RPM=120
SEC_MIN_INTERVAL_MS=200
CACHE_DIR=.cache/filings
AUDIT_LOG_PATH=audit-logs/tool-calls.jsonl
METRICS_PATH=metrics.jsonl
